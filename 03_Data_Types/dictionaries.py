# A dictionary (dict) is defined by using double brackets ({})
# A dictionary has items presented in key:value pairs, and can be referred to by using the key name.
my_dict = {
    'name': 'user',
    'email': 'user@email.com'
}
print(my_dict, type(my_dict))
